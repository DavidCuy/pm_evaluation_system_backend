
resume-quiz-person:
  handler: routes/api/v1/ResumeRouter.route_index
  events:
    - http:
        path: /api/v1/resume/{personId}/person/{quizId}/quiz
        method: get
        cors: ${self:custom.cors}

